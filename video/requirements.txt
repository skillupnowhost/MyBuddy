# Video generation environment — deliberately separate from backend/requirements.txt and
# from imagegen/requirements.txt. Install into its own venv (video/.venv) so the main API's
# dependencies stay light:
#
#   cd video
#   python -m venv .venv
#   .venv\Scripts\activate      # Windows
#   pip install -r requirements.txt
#
# GPU strongly required in practice. Text-to-video diffusion is dramatically heavier than
# text-to-image (many frames, each its own denoising process) — on CPU-only hardware this
# will not complete in any practical amount of time, not just "be slow." See README.md.

torch==2.6.0
diffusers==0.31.0
transformers==4.47.1
accelerate==1.2.1
safetensors==0.5.3
sqlalchemy==2.0.36
psycopg[binary]==3.2.3

# Frame sequence -> mp4 encoding (diffusers.utils.export_to_video, and edit_video.py's
# frame-by-frame reader/writer).
imageio==2.36.1
imageio-ffmpeg==0.5.1
pillow==12.1.0
numpy==2.3.0

# edit_video.py's REMOVE_BACKGROUND operation — CPU-only onnxruntime, no conflict with the
# torch/diffusers stack above. Unlike generation, this one actually completes on CPU-only
# hardware once this venv is provisioned; see README.md.
rembg==2.0.84
